<!DOCTYPE html>
<html class="scroll-smooth">
  <%= render "shared/head" %>

  <body class="selection:bg-slate-300 selection:text-slate-800 antialiased scroll-smooth">
    <main class="flex flex-row gap-12 w-full [--nav-width:18rem]">
      <%= render partial: "content/docs/sidebar_navigation", locals: { top_level_items: [{label: "Home", href: root_path, icon: "home"}, {label: "Library", href: resources_path, icon: "building-library", current_page?: current_page?(controller: "resources")}, {label: "Docs", href: articles_path, icon: "book-open", current_page?: current_page?(controller: "articles", action: "index")}], sections: Content::Article.sections } %>

     <div class="relative grow box-border flex-col w-full mx-auto lg:ml-(--nav-width) lg:px-6">
       <%= render partial: "content/docs/sidebar_navigation/toggle_button", locals: { resource: @resource } %>

        <%= yield %>
      </div>
    </main>
  </body>
</html>
